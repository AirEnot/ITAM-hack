# 📦 Инструкция по установке и запуску проекта

## 🎯 Что нужно сделать перед запуском

### 1. Установить Docker (если еще не установлен)

**Windows/Mac:**
- Скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop)
- Установите и запустите Docker Desktop

**Linux:**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo usermod -aG docker $USER
# Перезайдите в систему
```

**Проверка установки:**
```bash
docker --version
docker-compose --version
```

### 2. Клонировать репозиторий

```bash
git clone <repository-url>
cd ITAM-hack
```

### 3. (Опционально) Настроить переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username

# Секретный ключ (сгенерировать: openssl rand -hex 32)
SECRET_KEY=your_secret_key_here

# Админ (рекомендуется изменить!)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_secure_password_here
```

**Примечание:** Если `.env` не создан, будут использованы значения по умолчанию из `docker-compose.yml`.

---

## 🚀 Запуск проекта

### Шаг 1: Запустить Docker Compose

```bash
# Из корня проекта
docker-compose up --build
```

**Что происходит:**
1. Собираются Docker образы для backend и frontend
2. Создается база данных SQLite
3. Инициализируются таблицы
4. Создается первый админ
5. Запускаются оба сервиса

**Время первого запуска:** ~2-5 минут (зависит от скорости интернета)

### Шаг 2: Проверить, что все запустилось

Должны увидеть в консоли:
```
✅ Database initialized
✅ Админ создан: admin@example.com
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Шаг 3: Открыть в браузере

- **Frontend**: http://localhost
- **Backend API**: http://localhost:8000
- **API Документация**: http://localhost:8000/docs

---

## ✅ Проверка работы

### 1. Проверить Backend

Откройте http://localhost:8000/docs и попробуйте:
- `GET /health` - должен вернуть `{"status": "ok"}`
- `GET /api/hackathons` - должен вернуть список (может быть пустым)

### 2. Проверить Frontend

Откройте http://localhost и проверьте:
- Страница загружается
- Есть кнопка "Вход через Telegram"
- Нет ошибок в консоли браузера (F12)

### 3. Войти в админ-панель

1. Откройте http://localhost/admin/login
2. Введите:
   - Email: `admin@example.com` (или из вашего .env)
   - Password: `123123` (или из вашего .env)
3. Должна открыться админ-панель

---

## 🔧 Дополнительные настройки

### 1. Настроить Telegram бота

**Для авторизации участников через Telegram:**

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям и получите токен
4. Добавьте токен в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   TELEGRAM_BOT_USERNAME=your_bot_username
   ```
5. Перезапустите backend:
   ```bash
   docker-compose restart backend
   ```

### 2. Создать первый хакатон

1. Войдите в админ-панель: http://localhost/admin/login
2. Перейдите в "Управление хакатонами"
3. Нажмите "Создать хакатон"
4. Заполните форму:
   - Название
   - Описание
   - Дата начала
   - Дата окончания
   - Максимальный размер команды
5. Сохраните

### 3. Изменить пароль админа

**Важно для production!**

1. Создайте `.env` файл в корне проекта
2. Добавьте:
   ```env
   ADMIN_EMAIL=admin@example.com
   ADMIN_PASSWORD=your_very_secure_password
   ```
3. Перезапустите:
   ```bash
   docker-compose down
   docker-compose up -d
   ```

### 4. Сгенерировать SECRET_KEY

**Для production обязательно!**

```bash
# Linux/Mac
openssl rand -hex 32

# Windows (PowerShell)
[System.Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))

# Python
python -c "import secrets; print(secrets.token_hex(32))"
```

Добавьте в `.env`:
```env
SECRET_KEY=your_generated_secret_key_here
```

---

## 🛠️ Полезные команды

### Управление контейнерами

```bash
# Остановить
docker-compose down

# Запустить в фоне
docker-compose up -d

# Перезапустить
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Логи только backend
docker-compose logs -f backend

# Логи только frontend
docker-compose logs -f frontend
```

### Работа с БД

```bash
# Войти в контейнер backend
docker-compose exec backend bash

# Создать нового админа
docker-compose exec backend python add_admin.py

# Инициализировать БД заново (удалит все данные!)
docker-compose exec backend python init_db.py
```

### Очистка

```bash
# Остановить и удалить контейнеры
docker-compose down

# Удалить контейнеры + volumes (БД будет удалена!)
docker-compose down -v

# Полная очистка (контейнеры + volumes + образы)
docker-compose down -v --rmi all
```

---

## 🐛 Решение проблем

### Проблема: Порты заняты

**Ошибка:** `Bind for 0.0.0.0:8000 failed: port is already allocated`

**Решение:**
1. Найдите процесс, использующий порт:
   ```bash
   # Windows
   netstat -ano | findstr :8000
   taskkill /PID <PID> /F
   
   # Linux/Mac
   lsof -i :8000
   kill -9 <PID>
   ```
2. Или измените порты в `docker-compose.yml`:
   ```yaml
   ports:
     - "8001:8000"  # Backend на порту 8001
     - "8080:80"    # Frontend на порту 8080
   ```

### Проблема: БД не создается

**Решение:**
```bash
# Войти в контейнер
docker-compose exec backend bash

# Запустить инициализацию вручную
python init_db.py
```

### Проблема: CORS ошибки

**Ошибка:** `Access to XMLHttpRequest has been blocked by CORS policy`

**Решение:**
1. Добавьте ваш домен в `ALLOWED_ORIGINS` в `docker-compose.yml`:
   ```yaml
   ALLOWED_ORIGINS=http://localhost:80,http://your-domain.com
   ```
2. Перезапустите backend:
   ```bash
   docker-compose restart backend
   ```

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте, что backend запущен:
   ```bash
   docker-compose ps
   ```
2. Проверьте логи:
   ```bash
   docker-compose logs backend
   ```
3. Проверьте, что nginx проксирует запросы:
   - Откройте http://localhost/api/health
   - Должен вернуться `{"status": "ok"}`

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Полная очистка и пересборка
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: Изменения в коде не применяются

**Решение:**
```bash
# Пересобрать образы
docker-compose build --no-cache
docker-compose up -d
```

---

## 📚 Дополнительная документация

- **Быстрый старт**: [QUICKSTART.md](./QUICKSTART.md)
- **Подробная инструкция по Docker**: [DOCKER_SETUP.md](./DOCKER_SETUP.md)
- **Основной README**: [README.md](./README.md)
- **API Документация**: http://localhost:8000/docs (после запуска)

---

## ✅ Чеклист перед запуском

- [ ] Docker установлен и запущен
- [ ] Репозиторий клонирован
- [ ] (Опционально) Создан `.env` файл
- [ ] Запущен `docker-compose up --build`
- [ ] Backend доступен на http://localhost:8000
- [ ] Frontend доступен на http://localhost
- [ ] API Docs открываются на http://localhost:8000/docs
- [ ] Админ-панель доступна на http://localhost/admin/login
- [ ] (Опционально) Настроен Telegram бот
- [ ] (Опционально) Создан первый хакатон

---

**Готово! Проект должен работать. Удачи с хакатоном! 🚀**




# 🚀 ITAM Hack Platform

Веб-платформа для поиска и формирования команд для хакатонов в рамках хакатон сообщества ITAM.

---

## 📋 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Установка через Docker](#-установка-через-docker-рекомендуется)
3. [Локальная установка](#-локальная-установка-без-docker)
4. [Структура проекта](#-структура-проекта)
5. [Функционал](#-функционал)
6. [API Документация](#-api-документация)
7. [Решение проблем](#-решение-проблем)

---

## 🚀 Быстрый старт

### Вариант 1: Docker (Рекомендуется) ⭐

**Самый быстрый способ запустить проект:**

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd ITAM-hack

# 2. Запустить все сервисы (backend, frontend, telegram bot)
docker-compose up --build

# 3. Открыть в браузере
# Frontend: http://localhost
# Backend API: http://localhost:8000
# API Docs: http://localhost:8000/docs
```

**Подробная инструкция:** См. [QUICKSTART.md](./QUICKSTART.md) или [DOCKER_SETUP.md](./DOCKER_SETUP.md)

### Вариант 2: Локально

См. раздел [Локальная установка](#-локальная-установка-без-docker)

---

## 🐳 Установка через Docker (Рекомендуется)

### Требования

- **Docker** версии 20.10+
- **Docker Compose** версии 2.0+

### Шаг 1: Клонирование

```bash
git clone <repository-url>
cd ITAM-hack
```

### Шаг 2: Настройка переменных окружения (обязательно)

**Важно:** Перед запуском необходимо создать файл `.env` в корне проекта.

```bash
# Скопируйте пример файла
cp env.example .env

# Отредактируйте .env и заполните все необходимые переменные
# Особенно важно указать:
# - TELEGRAM_BOT_TOKEN (получить у @BotFather в Telegram)
# - SECRET_KEY (сгенерировать случайную строку)
# - ADMIN_EMAIL и ADMIN_PASSWORD
```

**Примечание:** Файл `.env` не должен попадать в git (уже добавлен в `.gitignore`). Все секретные данные должны храниться только в локальном `.env` файле.

### Шаг 3: Запуск

```bash
# Сборка и запуск всех сервисов
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

### Шаг 4: Проверка

- **Frontend**: http://localhost
- **Backend API**: http://localhost:8000
- **API Документация**: http://localhost:8000/docs

### Полезные команды

```bash
# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart
```

**Полная документация по Docker:** [DOCKER_SETUP.md](./DOCKER_SETUP.md)

---

## 💻 Локальная установка (без Docker)

### Требования

- **Python 3.10+**
- **Node.js 18+** и **npm**
- **Git**

### Backend

```bash
cd backend

# 1. Создать виртуальное окружение
python -m venv venv

# 2. Активировать
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Инициализировать БД
python -c "from database import init_db; init_db()"

# 5. Создать админа
python add_admin.py

# 6. Запустить сервер
uvicorn main:app --reload --port 8000
```

### Frontend

```bash
cd frontend

# 1. Установить зависимости
npm install

# 2. Запустить dev-сервер
npm run dev
```

Откройте http://localhost:3000 (или порт из vite.config.ts)

### Telegram Bot

```bash
cd backend

# 1. Убедитесь, что активировано виртуальное окружение
# 2. Создайте .env файл с TELEGRAM_BOT_TOKEN
# 3. Запустите бота
python telegram_bot.py
```

---

## 📂 Структура проекта

```
ITAM-hack/
├── docker-compose.yml          # Docker Compose конфигурация
├── .dockerignore               # Исключения для Docker
├── README.md                   # Этот файл
├── QUICKSTART.md               # Быстрый старт
├── DOCKER_SETUP.md             # Подробная инструкция по Docker
├── INSTALLATION.md             # Пошаговая инструкция по установке
│
├── backend/                    # Backend (FastAPI)
│   ├── Dockerfile              # Docker образ для backend
│   ├── Dockerfile.telegram     # Docker образ для Telegram бота
│   ├── main.py                 # Главный файл приложения
│   ├── config.py               # Конфигурация
│   ├── database.py             # Подключение БД
│   ├── models.py               # SQLAlchemy модели
│   ├── schemas.py              # Pydantic схемы
│   ├── init_db.py              # Скрипт инициализации БД
│   ├── add_admin.py           # Скрипт создания админа
│   ├── telegram_bot.py         # Telegram бот
│   ├── requirements.txt       # Python зависимости
│   ├── routers/                # API роутеры
│   │   ├── auth.py            # Аутентификация
│   │   ├── users.py           # Управление профилем
│   │   ├── hackathons.py       # Хакатоны
│   │   ├── teams.py           # Команды
│   │   ├── invitations.py     # Приглашения
│   │   └── admin.py           # Админ-панель
│   ├── services/               # Бизнес-логика
│   └── utils/                  # Утилиты
│
└── frontend/                   # Frontend (Vue 3 + TypeScript)
    ├── Dockerfile              # Docker образ для frontend
    ├── nginx.conf              # Конфигурация Nginx
    ├── package.json            # Node.js зависимости
    ├── vite.config.ts          # Конфигурация Vite
    └── src/                    # Исходный код
        ├── components/         # Vue компоненты
        ├── router/             # Vue Router
        ├── store/              # Pinia stores
        └── utils/              # Утилиты
```

---

## 🎯 Функционал

### Для участников

- ✅ **Регистрация через Telegram** - без паролей, через одноразовый код
- ✅ **Выбор и регистрация на хакатон** - просмотр доступных хакатонов
- ✅ **Создание анкеты** - навыки, опыт, желаемая роль
- ✅ **Просмотр других участников** - поиск сокомандников с фильтрами
- ✅ **Создание команды** - стать капитаном команды
- ✅ **Приглашение в команду** - отправка приглашений участникам
- ✅ **Принятие/отклонение приглашений** - управление входящими приглашениями
- ✅ **Управление командой** - добавление участников (для капитана)
- ✅ **Уведомления о приглашениях** - счетчик в header
- ✅ **Система не допускает участника в две команды** одного хакатона

### Для организаторов

- ✅ **Добавление/редактирование хакатонов** - полное управление через CMS
- ✅ **Просмотр участников** - все зарегистрировавшиеся анкеты
- ✅ **Просмотр команд** - отслеживание формирования команд
- ✅ **Аналитика** - статистика по хакатону в реальном времени
- ✅ **Экспорт данных** - CSV отчеты о составе команд и участников
- ✅ **Ручное распределение** - возможность вручную распределить участников по командам

---

## 📡 API Документация

После запуска backend откройте:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### 🔐 Аутентификация
- `POST /api/auth/telegram/verify-code` - Вход через Telegram (код)
- `POST /api/auth/admin/login` - Вход админа

#### 👤 Пользователи
- `GET /api/users/me` - Мой профиль
- `PUT /api/users/me` - Обновить профиль
- `GET /api/users/hackathons/{id}/participants` - Участники хакатона (с фильтрами)

#### 🎯 Хакатоны
- `GET /api/hackathons` - Список хакатонов
- `GET /api/hackathons/{id}` - Детали хакатона
- `POST /api/hackathons/{id}/register` - Регистрация на хакатон

#### 👥 Команды
- `GET /api/teams/my` - Мои команды
- `POST /api/teams` - Создать команду
- `GET /api/teams/{id}` - Детали команды
- `POST /api/teams/{id}/invite` - Пригласить в команду

#### 💌 Приглашения
- `GET /api/invitations` - Мои приглашения
- `POST /api/invitations/{id}/accept` - Принять/отклонить

#### 📊 Админ
- `GET /api/admin/hackathons` - Все хакатоны
- `POST /api/hackathons` - Создать хакатон
- `GET /api/admin/{id}/analytics` - Аналитика
- `GET /api/admin/{id}/participants/export` - Экспорт CSV

---

## 🔧 Что нужно сделать дополнительно

### 1. Настроить Telegram бота (для авторизации участников)

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен
3. Добавьте в `.env` или `docker-compose.yml`:
   ```env
   TELEGRAM_BOT_TOKEN=your_token_here
   TELEGRAM_BOT_USERNAME=your_bot_username
   ```
4. Перезапустите сервисы:
   ```bash
   docker-compose restart telegram-bot
   ```

### 2. Изменить пароль админа

**В Docker:**
```bash
# Создайте .env файл в корне проекта
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_secure_password
```

**Локально:**
```bash
# Отредактируйте backend/config.py или создайте backend/.env
ADMIN_PASSWORD=your_secure_password
```

### 3. Сгенерировать SECRET_KEY (для production)

```bash
# Linux/Mac
openssl rand -hex 32

# Windows (PowerShell)
[System.Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))

# Python
python -c "import secrets; print(secrets.token_hex(32))"
```

Добавьте в `.env`:
```env
SECRET_KEY=your_generated_secret_key_here
```

### 4. Создать первый хакатон

1. Войдите в админ-панель: http://localhost/admin/login
   - Email: `admin@example.com` (или из вашего .env)
   - Password: `123123` (или из вашего .env)
2. Перейдите в "Управление хакатонами"
3. Нажмите "Создать хакатон"
4. Заполните форму и сохраните

---

## 🐛 Решение проблем

### Docker

**Порты заняты:**
```bash
# Измените порты в docker-compose.yml
ports:
  - "8080:80"      # Frontend
  - "8001:8000"    # Backend
```

**БД не создается:**
```bash
docker-compose exec backend python init_db.py
```

**CORS ошибки:**
Добавьте ваш домен в `ALLOWED_ORIGINS` в `docker-compose.yml`

### Локально

**БД ошибки:**
```bash
# Удалить и пересоздать БД
rm backend/database.sqlite
python -c "from database import init_db; init_db()"
```

**Import ошибки:**
```bash
# Убедитесь, что venv активирован
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate    # Windows
pip install -r requirements.txt
```

---

## 🛠️ Технологии

- **Backend**: FastAPI, SQLAlchemy, SQLite, JWT
- **Frontend**: Vue 3, TypeScript, Vite, Pinia, Vue Router
- **Telegram Bot**: python-telegram-bot
- **Infrastructure**: Docker, Docker Compose, Nginx

---

## 📚 Дополнительная документация

- **Быстрый старт**: [QUICKSTART.md](./QUICKSTART.md)
- **Docker Setup**: [DOCKER_SETUP.md](./DOCKER_SETUP.md)
- **Установка**: [INSTALLATION.md](./INSTALLATION.md)
- **API Docs**: http://localhost:8000/docs (после запуска)

---

## 📝 Лицензия

MIT License

---

## 👨‍💼 Автор

Создано командой **BDC** для ITAM Hackathon Community

---

**Удачи с хакатоном! 🚀**
